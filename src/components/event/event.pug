mixin Event({name, date, datetime, type, image})
  article.events__event(tabindex="0")
    .event__preview(style=`background-image: url(${image})`)
    h3.event__name= name
    time.event__date(datetime=datetime)= date
    span.event__type= type